// PLACEHOLDER — será substituído por `npm run db:types`
// (supabase gen types typescript --local > packages/shared/src/database.types.ts)
// quando o Docker estiver instalado e a migration tiver rodado.
// Até lá, painel e mobile usam tipos escritos à mão (apps/*/src/types).
export type Database = unknown;
