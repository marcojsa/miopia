// ABA FAMÍLIA: lista de filhos, perfil e configurações.
// Placeholder estrutural — TODO: visual pós-aprovação dos mockups.
// TODO: listar children da família + atalhos para child/[childId], notifications-help e account.
import { Text, View } from 'react-native';

export default function FamilyIndexScreen() {
  return (
    <View>
      <Text>Família</Text>
      {/* Estrutura mínima:
          - lista de filhos -> /family/child/[childId]
          - "Lembretes atrasando?" -> /family/notifications-help
          - Conta -> /family/account */}
    </View>
  );
}
